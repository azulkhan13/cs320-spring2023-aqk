use "./../../../mysmlib/mysmlib-cls.sml";

val abs = fn(x: int) => if x>=0 then x else ~X

val quiz02_02 : (xs: int list, ys: int list) : bool =

    let fun help(x: int, y: int) : bool =

    val abs = fn (x : int) => x >= 0 then x else ~x

  


    